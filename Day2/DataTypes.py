# This program demonstrates the use of different data types in Python.
# It takes two numbers as input and calculates their sum.
# It also shows how to convert between different data types.

num = input('Enter two numbers: ')
sum = int(num[0]) + int(num[1])
print('The sum of the two numbers is: ' + str(sum))